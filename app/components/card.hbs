<div class="card" style="">
  <img src="{{@recipe.image}}" class="card-img-top" alt="...">
  <div class="card-body">
    <h5 class="card-title">{{@recipe.name}}</h5>
    <p class="card-text">{{@recipe.description}}</p>
  </div>
  <div class="card-body">
      <LinkTo @route="recipe" @model={{@recipe.id}}>View Recipe</LinkTo>
  </div>
</div>
{{log @recipe}}